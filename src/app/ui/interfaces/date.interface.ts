export  enum Week {
    Sunday,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
  
}

export enum Time {
  day = "day",month = "month",year="year"
}

export enum Operation {plus='plus',minus='minus'};
export type OperationType = 'plus' | 'minus'| '';